import module from '../module';

const cssPrefix = module.cssPrefix.childPrefix('actorSettings');

export const TOOLTIP_CELL = cssPrefix.child('tooltipToggleCell');
export const TOOLTIP_TOGGLE = cssPrefix.child('tooltipToggle');
